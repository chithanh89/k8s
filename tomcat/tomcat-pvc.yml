kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: tomcat-pvc
spec:
  storageClassName: local-storage
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 40Gi
  volumeName: tomcat-pv
